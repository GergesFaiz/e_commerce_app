class CategoryEntity {
  final String id, name, image;
  const CategoryEntity({required this.id, required this.name, required this.image});
}

class BrandEntity {
  final String id, name, image;
  const BrandEntity({required this.id, required this.name, required this.image});
}