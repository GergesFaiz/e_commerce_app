name: e_commerce
description: "Flutter e-commerce app with clean architecture, Bloc state management and the Route Misr REST API."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get_it: ^8.0.2
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  dartz: ^0.10.1
  google_fonts: ^6.2.0
  shared_preferences: ^2.2.3
  go_router: ^13.2.0
  json_annotation: ^4.9.0
  pretty_dio_logger: ^1.3.1
  flutter_screenutil: ^5.9.0
  cached_network_image: ^3.3.1
  fluttertoast: ^8.2.4
  connectivity_plus: ^5.0.2
  retrofit: 4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: 8.0.6
  json_serializable: ^6.8.0
  build_runner: ^2.4.9
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
